import React from 'react';

class Logout extends React.Component {
    constructor(props){
        super(props);
        // localStorage.clear();
    }
    render() {
        return <div>

                </div>
    }
}

export default Logout;